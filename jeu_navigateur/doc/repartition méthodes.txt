//---------------------------------------------------------
//REPARTITION DES METHODES DANS LES CLASSES
//---------------------------------------------------------


   // ----------------------------------------------------
   // PartieController
   // ----------------------------------------------------

       // invoquerAction
       // -----------------------------------------------
            teste la requete
            appelle herosActif->invoquer()
            met la db a jour 
            construis la vue

       // attaquerAction
       // -----------------------------------------------
            teste la requete
            appelle herosActif->attaquer()
            met la db a jour partie->update()
            construis la vue

       // passerTourAction
       // -----------------------------------------------
            teste la requete
            appelle partie->finDeTour()
            met la db a jour 
            construis la vue

       // AbandonAction
       // -----------------------------------------------
            teste la requete
            appelle partie->abandon()
            met la db a jour 
            construis la vue

       // partieTerminee
       // -----------------------------------------------
            statistiques joueur
            met la db a jour 
            redirige vers un autre page

       // createPartie
       // -----------------------------------------------
            si la partie n'existe pas, on la crée

       // loadPartie
       // -----------------------------------------------
            si la partie existe, on la charge

       // gestion de connexion
       // -----------------------------------------------
           là je sais pas trop mais a un moment il faudra
           tester que les joueurs connectés sont les bons


   // ----------------------------------------------------
   // Partie
   // ----------------------------------------------------


       // passerTour
       // -----------------------------------------------
            incremente partie->cptTour
            appelle herosActif()->finirTour()
            appelle herosInactif()->commencerTour()
            appelle partie->switchHeros()

       // update
       // -----------------------------------------------
            Lit le tableau updateList ( qui contient la 
            liste des objets à mettre a jour ) et met à 
            jour la DB

       // addToUpdateList
       // -----------------------------------------------
            ajoute l'objet passé en parametre à updateList

       // listerCliquable
       // -----------------------------------------------
            remplit le tableau cliquableList avec la liste
            des elements qui doivent être cliqucables en 
            fonction de la situation

       // isGameOver
       // -----------------------------------------------
            verifie si un joueur est mort 
                heros->isDead()
            verifie si un joueur est à court de carte
                heros->haveCarte()

       // switchHeros
       // -----------------------------------------------
            echange le contenu des attributs herosActif et
            herosInatctif


   // ----------------------------------------------------
   // Heros
   // ----------------------------------------------------

       // invoquer
       // -----------------------------------------------
            verifie si la carte a invoquer est bien dans la main ( isCardInZone)
            heros->canIBuy (on peut la pager?)
            heros->buy (soustrait le cout de la carte de la cagnotte)
            heros->deplacerCarte ( main) 
            
            si la carte a une methode qui se déclanche à l'invocation,
            on l'appelle carte->onInvoquation()
            
            partie->addToUpdate ( carte )


       // invoquer
       // -----------------------------------------------

       // attaquer
       // -----------------------------------------------

       // passerTour
       // -----------------------------------------------

       // subir
       // -----------------------------------------------

       // isDead
       // -----------------------------------------------

       // isCardInZone
       // -----------------------------------------------

       // changeCardZone
       // -----------------------------------------------

       // canIBuy
       // -----------------------------------------------

       // Buy
       // -----------------------------------------------

   // ----------------------------------------------------
   // Carte
   // ----------------------------------------------------

       // frapper 
       // -----------------------------------------------

       // subir
       // -----------------------------------------------

       // isDead
       // -----------------------------------------------

